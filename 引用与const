引用就是起别名，用法如下
int &ri = i;//为变量i取了ri的别名

const:
常量限定符，定义一个常量,用法如下：
const int Max = 99999999;//定义了常量Max，并初始化为99999999

对const的应用（常量应用）：
常量应用的也必须是一个常量,如下：
const int i = 10;
const int &ri = i; //实际在初始化常量引用时，等号右边可以不是常量，编译器回建立一个临时量来初始化常量引用。
